PACKAGE = @PACKAGE@
VERSION = @VERSION@

DIST_CONF = Makefile.am Makefile.in ${ACLOCAL} ${ACCONFIG} ${CONFIG_TOP} \
	${CONFIG_BOT}
DIST_FILES = $(DIST_CONF) $(SOURCES) $(HEADERS) $(TEXINFOS) $(INFOS) $(MANS) \
	$(DIST_OTHER) $(DIST_COMMON)

