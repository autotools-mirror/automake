install-ddata: $(DATA)
	$(top_srcdir)/mkinstalldirs $(datadir)
	for p in $(DATA); do			\
	  $(INSTALL_DATA) $(srcdir)/$$p $(datadir)/$$p; \
	done

uninstall-ddata:
	for p in $(DATA); do			\
	  rm -f $(datadir)/$$p;			\
	done

