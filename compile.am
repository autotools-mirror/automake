.c.o:
	$(COMPILE) $<

mostlyclean-compile:
## Don't remove 'core.*' because some distributions have eg "core.c".
	rm -f *.o core

clean-compile:

distclean-compile:
	rm -f *.tab.c

maintainer-clean-compile:
