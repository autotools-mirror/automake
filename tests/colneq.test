#! /bin/sh

# Test that := definitions work.

. $srcdir/defs || exit 1

cat > Makefile.am << 'END'
AUTOMAKE_OPTIONS = -Wno-portability
ICONS := $(wildcard *.xbm)
data_DATA = $(ICONS)
END

$ACLOCAL || exit 1
$AUTOMAKE
