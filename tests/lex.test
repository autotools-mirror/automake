#! /bin/sh

# Test to make sure that lex source generates correct target.

. $srcdir/defs || exit 1

cat > Makefile.am << 'END'
bin_PROGRAMS = zot
zot_SOURCES = joe.l
END

: > joe.l

$AUTOMAKE || exit 1

grep '\$(LEX)' Makefile.in
