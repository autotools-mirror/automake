#! /bin/sh

# Test to make sure all-local works correctly.

. $srcdir/defs || exit 1

cat > Makefile.am << 'END'
all-local:
	@echo foo
END

$AUTOMAKE || exit 1
grep 'all-local all-local' Makefile.in && exit 1
exit 0
