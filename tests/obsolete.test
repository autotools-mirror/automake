#! /bin/sh

# Test to make sure obsolete macros really are.

. $srcdir/defs || exit 1

cat >> configure.in << 'END'
fp_PROG_INSTALL
END

: > Makefile.am

$ACLOCAL || exit 1
$AUTOMAKE && exit 1
exit 0
