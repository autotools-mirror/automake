#! /bin/sh

# Make sure macros defined in acinclude.m4 are always found correctly.
# Report from Jim Meyering.

. $srcdir/defs || exit 1

cat > configure.in << 'END'
jm_TEST
END

cat > acinclude.m4 << 'END'
AC_DEFUN([jm_TEST])
AC_REQUIRE([jm_TEST])
END

$ACLOCAL
