#! /bin/sh

# Test to make sure LIBOBJS actually set in configure.in if seen.

. $srcdir/defs || exit 1

cat > Makefile.am << 'END'
noinst_LIBRARIES = tu
tu_SOURCES =
tu_LIBADD = @LIBOBJS@
END

$AUTOMAKE && exit 1
exit 0
