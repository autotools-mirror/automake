#! /bin/sh

# Test exe-specific flags with dependency tracking.

. $srcdir/defs || exit 1

cat >> configure.in << 'END'
AC_PROG_CC
END

cat > Makefile.am << 'END'
bin_PROGRAMS = foo
foo_SOURCES = foo.c
foo_CFLAGS = -DBAR
END

: > compile

$AUTOMAKE || exit 1

grep 'CC.*-Wp' Makefile.in
